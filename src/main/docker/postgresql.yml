name: service
services:
  postgresql:
    image: postgres:latest
    environment:
      POSTGRES_DB: service_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - 127.0.0.1:5432:5432
