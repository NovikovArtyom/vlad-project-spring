version: '3.1'

services:
  db:
    image: postgres:latest
    container_name: db-dev
    ports:
      - "6541:5432"
    environment:
      - POSTGRES_DB=service-db-dev
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=pwKj9fzR